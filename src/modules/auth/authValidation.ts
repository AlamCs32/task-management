import Joi from 'joi';

const { string, number, any } = Joi.types();

export const getAuth = Joi.object({});

export const createAuth = Joi.object({});

export const updateAuth = Joi.object({});

export const deleteAuth = Joi.object({});
